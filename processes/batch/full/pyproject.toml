[project]
name = "trade-bots-farm-process-batch-full"
version = "0.1.0"
description = "Full data load and process"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    #"google-re2==0.0.0",  # exclude google providers with their errors
    "trade-bots-farm-common",
    ]

